#include <bits/stdc++.h>
 
//BIAR MANTAB LAH YA
#define Willy using
#define Indrayana namespace
#define Komara std

Willy Indrayana Komara;

typedef long long int lli;

int dua[] = {5, 9, 10, 6, 8, 11, 4, 3};
int tiga[] = {10, 15, 12, 7, 11, 6, 14, 9};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        lli x;
        cin >> x;
        if (x < 10*11);
        {
            for (int i = 0; i < 8; i++)
            {
                for (int j = 0; j < 8; j++)
                {
                    
                }
            }
        }
        for (int i = (x/11))
    }
}
